import React from "react";

/**
 * 中文：预留的首页组件；当前应用默认路由由 App 导向 Dashboard。
 * English: Placeholder home component; App currently redirects the default route to Dashboard.
 */
function HomePage() {
  return <main>Home Page</main>;
}

export default HomePage;
